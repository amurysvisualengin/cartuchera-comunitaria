/* import { useContext } from "react";
import CardListContext from "../../context/CardListContext";

const CardShuffleMobile = (props) => {
  const { isMobile, setIsMobile } = useContext(CardListContext);
};

export default CardShuffleMobile;
 */
